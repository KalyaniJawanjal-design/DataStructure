class Solution {
    public int[] sortedSquares(int[] A) {
        int b[] = new int[A.length];
        int i = 0;
        for(int x:A){
            b[i] = x*x;
            i++;
        }
        Arrays.sort(b);
        return b;
    }
}
